// doctest entry point — provides main() and the single implementation of the
// framework. Every other test_*.cpp only includes the header.
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest/doctest.h"
